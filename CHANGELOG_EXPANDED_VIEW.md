# Changelog - Развёрнутый просмотр писем

## [Unreleased] - 2025-11-27

### Добавлено ✨

- **Развёрнутый просмотр письма**: Двойной клик по окну уведомления разворачивает его и показывает полное содержимое письма
- **Настройки размеров развёрнутого окна**: Два новых поля в настройках для указания желаемых размеров (ширина и высота)
- **Поддержка HTML писем**: Полная поддержка HTML содержимого с изображениями, ссылками, таблицами
- **Поддержка текстовых писем**: Отображение plain text писем
- **Автоматическая прокрутка**: Длинные письма можно прокручивать в развёрнутом виде
- **Извлечение содержимого из Gmail API**: Backend получает полное содержимое письма при запросе уведомления
- **Декодирование Base64**: Автоматическое декодирование URL-safe Base64 содержимого от Gmail

### Исправлено 🔧

- **HTML entities в полях "От:" и "Кому:"**: Угловые скобки в email адресах теперь отображаются корректно (`<` вместо `&lt;`)
- **Отступ у поля "Кому:"**: Убран лишний левый отступ, теперь поле выровнено с "От:"
- **Размер окна**: Окно корректно изменяет размер при разворачивании/сворачивании

### Изменено 🔄

- **Gmail API запрос**: Формат изменён с `metadata` на `full` для получения полного содержимого письма
- **Структура данных**:
  - Backend: Добавлено поле `body: Option<String>` в `GmailNotification`
  - Frontend: Добавлено поле `body?: string | null` в `NotificationPayload`
- **Настройки**: Добавлены ��оля `notification_expanded_width` и `notification_expanded_height` (значения по умолчанию: 800×600)

### Технические детали 🔧

#### Backend (Rust)
- `src-tauri/src/config.rs`:
  - Добавлены поля в `Settings` и `SettingsUpdate`
  - Добавлена валидация размеров развёрнутого окна
- `src-tauri/src/gmail.rs`:
  - Добавлено поле `body` в `GmailNotification`
  - Изме��ён метод `fetch_message()` для получения полного содержимого
  - Добавлены структуры `MessagePart` и `MessageBody` для парсинга MIME
  - Добавлены функции `extract_body()`, `find_part_by_mime()`, `decode_body()`
  - Добавлен импорт `base64` для декодирования

#### Frontend (Angular)
- `frontend/src/app/components/notification-overlay/notification-overlay.ts`:
  - Добавлен signal `isExpanded` для отслеживания состояния
  - Добавлен метод `toggleExpand()` для изменения размера окна
  - Добавлено поле `body` в тип `NotificationPayload`
  - Добавлен импорт `LogicalSize` из Tauri API
  
- `frontend/src/app/components/notification-overlay/notification-overlay.component.html`:
  - Добавлен обработчик `(dblclick)="toggleExpand()"`
  - Добавлен блок `.alert-body` с `[innerHTML]` для HTML содержимого
  - Условное отображение snippet только в свёрнутом виде
  
- `frontend/src/app/components/notification-overlay/notification-overlay.component.scss`:
  - Убран `margin-left` у `.alert-recipient`
  - Добавлены стили для `.alert-body` с прокруткой и форматированием HTML элементов
  
- `frontend/src/app/components/settings-page/`:
  - Добавлены поля ввода для размеров развёрнутого окна
  - Обновлена модель настроек
  - Обновлена логика сохранения

### Зависимости 📦

- Используется существующая зависимость `base64 = "0.22"` в `Cargo.toml`
- Не требуется установка дополнительных пакетов

### Миграция данных 📊

Существующие настройки автоматически дополняются значениями по умолчанию:
```rust
notification_expanded_width: 800,
notification_expanded_height: 600,
```

Пользователи могут изменить эти значения в настройках приложения.

### Производительность ⚡

- Gmail API теперь запрашивает полное содержимое письма вместо только метаданных
- Это может немного увеличить время загрузки уведомления (на ~100-300ms)
- Содержимое кэшируется на стороне клиента после получения
- Декодирование Base64 выполняется быстро и не влияет на производительность

### Безопасность 🔒

- HTML содержимое санируется Angular через `[innerHTML]`
- Потенциально опасные скрипты автоматически удаляются
- Изображения загружаются из оригинального источника (Gmail)

### Известные ограничения ⚠️

- Вложения (attachments) пока не поддерживаются - планируется в будущих версиях
- Очень большие письма могут замедлить отображение - рекомендуется использовать кнопку "Перейти"
- Некоторые сложные HTML письма могут отображаться не идеально - CSS стили ограничены

### Документация 📖

- Создан `docs/EXPANDED_VIEW.md` - полная техническая документация
- Создан `docs/USER_GUIDE_EXPANDED_VIEW.md` - руководство пользователя
- Обновлён `docs/NOTIFICATION_IMPROVEMENTS.md`

### Тестирование ✅

- [x] Компиляция Rust кода без ошибок
- [x] Сборка Frontend без ошибок
- [ ] Ручное тестирование разворачивания окна
- [ ] Тестирование с HTML письмами
- [ ] Тестирование с текстовыми письмами
- [ ] Тестирование сохранения настроек
- [ ] Проверка на разных разрешениях экрана

### Планы на будущее 🚀

- [ ] Добавить кнопку развёртывания/сворачивания (помимо двойного клика)
- [ ] Запоминать состояние развёрнутости между уведомлениями  
- [ ] Добавить анимацию при изменении размера окна
- [ ] Поддержка вложений (attachments) с предпросмотром
- [ ] Предпросмотр изображений в полном размере по клику
- [ ] Настройка приоритета HTML vs plain text
- [ ] Экспорт письма в PDF

